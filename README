stdout系のパッチをなんとかする(another_stdout?) -> reduce_text pluginは今は使えない
spec通らないのをなんとかする -> とりあえずsort!
streamでtypable idを
重複したmessageは表示しないように
RT元のmessageがあればtypable idに変換
diff_followでspam度やbot度でsort
diff_followでspamやbotの個人辞書/共有辞書を
URLは改行しない linefeed plugin
xterm内にicon表示(w3mっぽく) -> get_icon_pathがDRYじゃない, gif/png anime ON/OFF, refresh
truncateでなんもなしupdateをするとNo Method Error
linuxだと入力中の更新が止まっていないように見える
streamでfriendsと全く無関係な人のTLが流れてくる
streamで自分のTLに流れてこないTLのみ流す(not friend <-> friendの会話を見る)
streamをDRYに
立ち上げ時に r を実行したい
keywordでのpolling pluginを探して取り込む
自分以外の friends を見たときは storage に入れないようにする
fibが立ち上げ時にCPUを使うのを抑制(ただの実験ブランチ)
streamをthread safeに
顔文字ジェネレータ
last.fm
特定キーワード(地震とか)をまとめて位置情報をつけて出力
メモリ増加問題 -> DB化とは別の視点で考えたい
setでパスワードがまる見え問題
mixi echo?
tail -fとかをfifoかUDPで取り込む機能
update draft queue
aa_profileブランチをpython-aafigure対応させる
新規pluginはgemspecに追加しないといけない?
空気を読むCTRL-L
more plugin ("32 new tweets." のようなやつ)
コピペモード (改行コードでupdateされないような仕掛け。しかし、どう実現する?)
